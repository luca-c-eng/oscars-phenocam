# ftp_credentials.txt (positional format)
# One value per line. Empty lines and comments (#) are ignored.
# DO NOT commit this file to any repository — it contains plain-text credentials.
# Create it manually on the RPi during installation.

# 1) FTP_HOST — IP address or hostname of the FTP server
192.168.1.100

# 2) FTP_PORT — FTP server port (standard: 21; non-standard ports are supported)
21

# 3) FTP_REMOTE_BASE — base path on the server (must already exist)
#    The software appends SITENAME/YYYY_MM_DD/ automatically as subfolders.
/phenocams/data

# 4) FTP_USER — FTP username
your_ftp_username

# 5) FTP_PASS — FTP password
your_ftp_password
